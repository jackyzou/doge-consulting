# ─── Contact Form (SMTP Email) ──────────────────────────────────
# These are required for the contact form to actually send emails.
# Without them the form still works but messages are only logged to the console.

# Gmail example:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=dogetech77@gmail.com
#   SMTP_PASS=xxxx xxxx xxxx xxxx        ← Gmail App Password (not your login password)
#
# To generate a Gmail App Password:
#   1. Go to https://myaccount.google.com/apppasswords
#   2. Select "Mail" and your device
#   3. Copy the 16-character password

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=dogetech77@gmail.com
SMTP_PASS=

# Where to receive contact form submissions (defaults to dogetech77@gmail.com)
CONTACT_RECEIVER_EMAIL=dogetech77@gmail.com

# ─── Airwallex Payment Gateway ─────────────────────────────────
# Sign up at https://www.airwallex.com and get API credentials
# For demo/sandbox: use the demo environment keys
# Without these keys, payment will run in demo mode (no real charges)

AIRWALLEX_CLIENT_ID=
AIRWALLEX_API_KEY=
AIRWALLEX_WEBHOOK_SECRET=

# Set to "production" for live payments, omit or set to "demo" for sandbox
NEXT_PUBLIC_AIRWALLEX_ENV=demo
